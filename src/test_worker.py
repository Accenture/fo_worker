#!/usr/bin/env python
# -*- coding: utf-8 -*-
from unittest import TestCase

# def fake_runner(body) -> object:
#     LOGGER.info('Child process running: %s, %s', getpid(), body)
#     duration = 5
#     for i in range(duration):
#         LOGGER.info('pretending to do work... (%s/%s)', i + 1, duration)
#         sleep(1)
#     res = dict(
#         job_id='the_job_id',
#         user_id='the_user_id',
#         outcome='the_outcome'
#     )
#     LOGGER.info('End of child Process %s', getpid())
#     return json.dumps(res)


class TestMain(TestCase):
    pass
